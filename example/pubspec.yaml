name: search_test
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  search_app_bar:
    path: ../
  rxdart: ^0.27.1
  diacritic: ^0.1.3
  cupertino_icons: ^0.1.3
  dio: ^4.0.0
  json_annotation:

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  json_serializable:

flutter:
  uses-material-design: true
